import { DemarcationDirective } from './demarcation.directive';

describe('DemarcationDirective', () => {
  it('should create an instance', () => {
    const directive = new DemarcationDirective();
    expect(directive).toBeTruthy();
  });
});
