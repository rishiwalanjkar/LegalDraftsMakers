import { EnableAddRemovePipe } from './enable-add-remove.pipe';

describe('EnableAddRemovePipe', () => {
  it('create an instance', () => {
    const pipe = new EnableAddRemovePipe();
    expect(pipe).toBeTruthy();
  });
});
