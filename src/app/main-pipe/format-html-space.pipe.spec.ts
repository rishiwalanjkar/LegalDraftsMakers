import { FormatHtmlSpacePipe } from './format-html-space.pipe';

describe('FormatHtmlSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatHtmlSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
