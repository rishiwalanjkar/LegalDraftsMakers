import { MatDividedInputPipe } from './mat-divided-input.pipe';

describe('MatDividedInputPipe', () => {
  it('create an instance', () => {
    const pipe = new MatDividedInputPipe();
    expect(pipe).toBeTruthy();
  });
});
