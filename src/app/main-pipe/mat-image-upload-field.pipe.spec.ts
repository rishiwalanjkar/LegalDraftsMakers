import { MatImageUploadFieldPipe } from './mat-image-upload-field.pipe';

describe('MatImageUploadFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new MatImageUploadFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
