import { BuildMatTableDataPipe } from './build-mat-table-data.pipe';

describe('BuildMatTableDataPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildMatTableDataPipe();
    expect(pipe).toBeTruthy();
  });
});
